return {
  -- Uncomment if debuggind need to see dependnecies
  -- "nvim-telescope/telescope.nvim",
  -- opts = function(_, opts)
  --   opts.pickers = opts.pickers or {}
  --   opts.pickers.find_files = {
  --     hidden = true,
  --     no_ignore = true,
  --     follow = true,
  --   }
  -- end,
}
